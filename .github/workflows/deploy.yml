name: Deploy Next.js site to Pages

on:
  # Runs on pushes targeting the main branch
  push:
    branches:
      - main

  # Allows you to run this workflow manually from the Actions tab
  workflow_dispatch:

# Sets permissions of the GITHUB_TOKEN to allow deployment to GitHub Pages
permissions:
  contents: read
  pages: write
  id-token: write

# Allow only one concurrent deployment, skipping runs queued between the run in-progress and latest queued.
# However, do NOT cancel in-progress runs as we want to allow these production deployments to complete.
concurrency:
  group: "pages"
  cancel-in-progress: true

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v7

      # 1. Install pnpm
      - uses: pnpm/action-setup@v6
        name: Install pnpm
        with:
          # Don't specify version here, to use the `packageManager` from `package.json`
          run_install: false

      # 2. Checkout original assets into folder /private
      - name: Checkout Private Assets
        uses: actions/checkout@v7
        with:
          repository: aimryoui/portfolio-assets
          token: ${{ secrets.ASSETS_PAT }}
          path: private

      - name: Install Node.js
        uses: actions/setup-node@v6
        with:
          node-version: 24.15.0
          cache: "pnpm"

      - name: Install dependencies
        run: pnpm install

      # 3. Install FFmpeg
      - name: Install FFmpeg
        uses: FedericoCarboni/setup-ffmpeg@v3
        with:
          architecture: ""
          github-token: ${{ github.server_url == 'https://github.com' && github.token || '' }}

      - name: Setup Pages
        id: setup_pages
        uses: actions/configure-pages@v6

      - name: Cache Next.js
        uses: actions/cache@v6
        with:
          path: .next/cache
          key: ${{ runner.os }}-nextjs-${{ hashFiles('**/pnpm-lock.yaml') }}-${{ hashFiles('**.[jt]s', '**.[jt]sx') }}
          restore-keys: |
            ${{ runner.os }}-nextjs-${{ hashFiles('**/pnpm-lock.yaml') }}-
            ${{ runner.os }}-nextjs-

      # 4. Media cache
      - name: Cache Images
        uses: actions/cache@v6
        with:
          path: |
            src/lib/image-manifest.json
            public/assets/media/**/*_scrambled.webp
            public/assets/media/**/*_preview.webp
          key: ${{ runner.os }}-image-${{ hashFiles('private/media/**/*.{png,jpg,jpeg,webp}', '!private/media/**/*-poster.webp') }}-${{ hashFiles('src/scripts/process-images.ts') }}
          restore-keys: |
            ${{ runner.os }}-image-

      - name: Cache Videos
        uses: actions/cache@v6
        with:
          path: |
            src/lib/video-manifest.json
            public/assets/media/**/index.txt
            public/assets/media/**/init.mp4
            public/assets/media/**/*.bin
            public/assets/media/**/*_preview.webp
          key: ${{ runner.os }}-video-${{ hashFiles('private/media/**/*.{mp4,mov,webm,gif}') }}-${{ hashFiles('private/media/**/*-poster.webp') }}-${{ hashFiles('src/scripts/process-videos.ts') }}
          restore-keys: |
            ${{ runner.os }}-video-

      - name: Cache Ambient Colors
        uses: actions/cache@v6
        with:
          path: public/color-manifest.json
          key: ${{ runner.os }}-color-${{ hashFiles('private/media/**/*') }}-${{ hashFiles('src/content/projects/**/*.mdx') }}-${{ hashFiles('src/scripts/process-colors.ts') }}
          restore-keys: |
            ${{ runner.os }}-color-

      # 5. Build
      - name: Build with Next.js
        run: pnpm run build
        env:
          NEXT_PUBLIC_BASE_URL: ${{ steps.setup_pages.outputs.origin }}
          PAGES_BASE_PATH: ${{ steps.setup_pages.outputs.base_path }}

      - name: Upload artifact
        uses: actions/upload-pages-artifact@v5
        with:
          path: ./out

  deploy:
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    runs-on: ubuntu-latest
    needs: build
    steps:
      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v5
